import { ValidateFunction } from 'ajv';
import { Schema } from './schema';
export default class Base<T extends Schema> {
    server: string;
    api: string;
    validate?: ValidateFunction<T> | undefined;
    token?: string | undefined;
    isPlural: boolean;
    constructor(server: string, api: string, validate?: ValidateFunction<T> | undefined, token?: string | undefined);
    protected val(d: unknown): void;
}

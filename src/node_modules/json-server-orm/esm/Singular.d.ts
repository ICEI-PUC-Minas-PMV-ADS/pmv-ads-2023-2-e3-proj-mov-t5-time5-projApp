import Base from './Base';
import { KVO, SingularSchema } from '.';
export default class Singular<T extends SingularSchema> extends Base<T> {
    isPlural: boolean;
    one(): Promise<T & KVO>;
    update(data: T & KVO, override?: boolean): Promise<T & KVO>;
}
